/* 初期処理 */
int init (struct parameter *, struct player_parameter *);
/* ファイルから状態をロード */
int load_data (struct parameter *, struct player_parameter *);
/* ファイルに状態をセーブ */
int save_data (struct parameter *, struct player_parameter *);
